{
  "name": "sub-store-convert",
  "version": "2.20.29",
  "module": "./packages/core/build/index.js",
  "license": "MIT",
  "exports": {
    ".": {
      "import": "./packages/core/build/index.js"
    }
  },
  "files": [
    "packages/core/build"
  ],
  "scripts": {
    "build": "pnpm --filter @sub-store/core build",
    "build:docker": "docker build -t ghcr.io/tbxark/sub-store-convert:latest .",
    "start": "pnpm --filter @sub-store/app start",
    "start:bun": "pnpm --filter @sub-store/app start:bun",
    "start:node": "pnpm --filter @sub-store/app start:node",
    "start:docker": "docker run --rm --name sub-store-convert -p 3000:3000 ghcr.io/tbxark/sub-store-convert:latest",
    "deploy": "pnpm --filter @sub-store/app deploy"
  },
  "devDependencies": {
    "esbuild": "^0.25.10",
    "wrangler": "^3.114.15"
  }
}
